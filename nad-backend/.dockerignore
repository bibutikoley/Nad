# Secrets — the container gets these from docker-compose.yml's `environment:`,
# never a mounted/copied file.
.env
.env.*
!.env.example

.venv/
.git/
__pycache__/
*.py[cod]

# Not needed by the token-server image, which only copies pyproject.toml, uv.lock
# and token_server.py.
README.md
scripts/
agent.py
noise_gate.py
stt_server.py
test_*.py
livekit.yaml
docker-compose.yml
