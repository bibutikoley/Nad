# Secrets — the container gets these from docker-compose.yml's `environment:`,
# never a mounted/copied file.
.env
.env.*
!.env.example

.venv/
.git/
__pycache__/
*.py[cod]

# Not needed by the token-server image.
README.md
scripts/
agent.py
livekit.yaml
docker-compose.yml
